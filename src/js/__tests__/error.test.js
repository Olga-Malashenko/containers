import Settings from '../app';

/* test.each([
    ['impossible option', ],
    ['invalid theme', ],
    ['invalid music', ],
    ['invalid difficulty', ]
])
('Should be trown error about %s', () => {
    const testSettings = new Settings();
});
 */
